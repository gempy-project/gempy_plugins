Property Estimation
----------------------

Populates a computed structural model's regular grid with a property (porosity, ore
grade, etc.) via GSTools kriging or simulation, respecting the model's lithology and
fault-block boundaries.
